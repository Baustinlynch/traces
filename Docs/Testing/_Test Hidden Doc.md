# Hidden Doc

This document is registered with `hidden: true` in `site-data.js`.

- It does **not** appear in the sidebar navigation.
- It is **still routable** by URL: `#/docs/Testing/Test Hidden Doc`.
- It cannot be reached via prev/next pagination.

Use it to test the hidden-doc behavior (like the real `Organiser main doc`).